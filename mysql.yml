---
  - name: Install MySQL
    hosts: localhost
    become: yes
    #become_user: root
    gather_facts: true
    tasks:
    - name: apt  Install mysql package in ansible
      package:
        name: mysql-server
        state: present
