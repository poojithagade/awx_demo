---
  - name: Install MySQL
    hosts: localhost
    become: yes
    become_user: root
    
    tasks:
    - name: apt  Install mysql package in ansible
      apt:
        name: apache2
        state: present

