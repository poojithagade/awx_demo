---
  - name: Install MySQL
    hosts: localhost
    gather_facts: true
    become: yes
    become_user: root
    tasks:
    - name: apt  Install mysql package in ansible
      ansible.builtin.yum:
        name: mysql-server
        state: present
